version = 1

cloudstream {
    description = "HHKungfu catalog, metadata and episode index (no protected stream extraction)"
    authors = listOf("Private HH Repo")
    status = 1
    tvTypes = listOf("Anime")
    language = "vi"
}

android {
    namespace = "com.hhkungfu"
}
